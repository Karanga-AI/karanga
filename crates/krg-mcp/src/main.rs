//! Karanga MCP server (interface §6–§7): exposes the read/write verbs to agents
//! over MCP, returning the lean projections.
//!
//! Scaffold: not implemented yet.

fn main() {
    eprintln!(
        "karanga MCP server (format v{}, interface v{}) — not implemented yet (scaffold).",
        krg_core::FORMAT_VERSION,
        krg_core::INTERFACE_VERSION
    );
    std::process::exit(2);
}
